cmake_minimum_required(VERSION 2.8)
project( test-main )
include_directories(include)
find_package( OpenCV REQUIRED )
file(GLOB SOURCES "src/*")
add_executable(test-main test/test-main.cpp ${SOURCES})
target_link_libraries( test-main ${OpenCV_LIBS} )
